#include <iostream>
using namespace std;
int main(){
	///we have to calculate summation.
	//range(limits) are 1 to 100
	 int summation;
	for (int k=1;k<=100;k++){
		summation+=k;//adding the values of k
	}
	cout<<"The summation on k is:"<<summation;
	return 0;
}
